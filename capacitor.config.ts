import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.ionicphotogallery',
  appName: 'Ionic Photo Gallery',
  webDir: 'dist',
};

export default config;
